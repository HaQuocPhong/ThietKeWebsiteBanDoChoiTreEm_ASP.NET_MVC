@model IEnumerable<DoChoiTreEmWeb.Models.DANHMUC>
<ul class="list-group">
    <li class="list-group-item active" style="font-weight:bold; text-align:center">DANH MỤC SẢN PHẨM</li>
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SanPhamTheoDanhMuc","DoChoiTreEm",new {id = item.MaDM})"
           class="list-group-item">@item.TenDM</a>
        //   <div class="list-group-item">
        //      @Html.ActionLink(item.TenChuDe, " SachTheoChuDe", new { id = item.MaCD }, "SachOnline")
        //  </div>
    }
</ul>
